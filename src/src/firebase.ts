// Deprecated duplicate: re-export the centralized firebase utilities from `src/firebase.ts`
// This file exists for backward compatibility. Prefer importing from `src/firebase.ts`.
export * from '../firebase';
export { default } from '../firebase';
